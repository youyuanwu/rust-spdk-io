[package]
name = "spdk-io-build"
version = "0.1.0"
edition.workspace = true
authors.workspace = true
documentation.workspace = true
repository.workspace = true
license.workspace = true
description = "Build helper utilities for SPDK Rust bindings with proper whole-archive support"

[dependencies]
